# OpenCode Example

A minimal abox instance configured for running [Open Code](https://opencode.ai/) in an isolated environment.

## Setup

```bash
cd examples/opencode
abox up
```

## Usage

```bash
abox ssh opencode
opencode  # Start OpenCode
```
